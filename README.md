https://github.com/RayRayLey/weblarek
# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные

#### Интерфейс IProduct
Содержит данные товара.

interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}

Поля интерфейса:
`id: string` - поле для хранения уникального идентификатора товара.
`description: string` - поле для хранения текстового описания товара.
`image: string` - поле для хранения ссылки на изображение товара.
`title: string` - поле для хранения текстового названия товара.
`category: string` - поле для хранения категории, к которой относится товар.
`price: number | null` - поле для хранения стоимости товара или учета её отсутсвия.

#### Интерфейс IBuyer
Содержит данные покупателя.

interface IBuyer {
  payment: TPayment;
  email: string;
  phone: string;
  address: string;
} 

Поля интерфейса:
`payment: TPayment` - поле для хранения способа оплаты заказа покупателя.
`email: string` - поле для хранения адреса электронной почты покупателя.
`phone: string` - поле для хранения телефонного номера покупателя.
`address: string` - поле для хранения адреса доставки.

### Модели данных

#### Класс Catalogue
Хранит массив всех товаров и товар, выбранный для подробного отображения.

Конструктор класса не принимает параметров.

Поля класса:  
`items: IProduct[]` -  массив всех товаров.
`itemDetails: IProduct | undefined` -  товар, выбранный для подробного отображения.

Методы класса:  
`saveItems(items: IProduct[]): void` - сохранение массива товаров полученного в параметрах метода.
`getItems(): IProduct[]` - получение массива товаров из модели.
`findItem(id: string): IProduct | undefined` - получение одного товара по его id.
`saveDetails(item: IProduct): void` - сохранение товара для подробного отображения.
`getDetails(): IProduct | undefined` - получение товара для подробного отображения.

#### Класс Basket
Хранит массив товаров, выбранных покупателем для покупки.

Конструктор класса не принимает параметров.

Поля класса:  
`items: IProduct[]` -  массив товаров.

Методы класса:  
`getItems(): IProduct[]` - получение массива товаров, которые находятся в корзине.
`addItem(item: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины.
`removeItem(item: IProduct): void` - удаление товара, полученного в параметре из массива корзины.
`clearBasket(): void` - очистка корзины.
`getWholePrice(): number` - получение стоимости всех товаров в корзине.
`getItemAmount(): number` - получение количества товаров в корзине.
`checkItem(id: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода.

#### Класс Buyer
Хранит данные покупателя.

Конструктор класса не принимает параметров.

Поля класса:  
`payment: TPayment` - поле для хранения способа оплаты заказа покупателя.
`address: string` - поле для хранения адреса доставки.
`phone: string` - поле для хранения телефонного номера покупателя.
`email: string` - поле для хранения адреса электронной почты покупателя.

Методы класса:  
`saveBuyer(payment: TPayment | '', address: string | '', phone: string | '', email: string | ''): void` - сохранение данных в модели.
`getBuyer()` - получение всех данных покупателя.
`clearBuyer(): void` - очистка данных покупателя.
`validator(): {payment?: string, address?: string, phone?: string, email?: string}` - валидация данных.

### Слой коммуникации

#### Класс ApiService
Использует композицию, чтобы выполнить запрос на сервер с помощью метода get класса Api и получает с сервера объект с массивом товаров.

Конструктор:  
`constructor(API: IApi)` - В конструктор передается объект, соответствующий интерфейсу IApi.

Поля класса:  
`API: IApi` - объект API, который используется для методов класса.

Методы:
Используются методы класса Api.
`getProducts(): Promise<ProductsList>` - выполняет GET запрос и возвращает промис с объектом, которым ответил сервер.
`sendData( data: OrderList, method: ApiPostMethods = 'POST')` - принимает объект с данными и отправляет эти данные на сервер.

### Представление

#### Интерфейс IBasket
Содержит данные для отображения корзины товаров.

interface IBasket {
  price: string;
  basketList: HTMLElement[];
} 

Поля интерфейса:
`price: string` - поле для хранения суммы стоимостей товаров в корзине.
`basketList: HTMLElement[]` - поле для хранения списка самих товаров.

#### Класс BasketView
Содержит логику отображения данных в корзине и взаимодействие с кнопкой заказа.

Конструктор:  
`constructor(protected events: IEvents, container: HTMLElement)` - В конструктор передается обработчик событий и унаследованный элемент контейнера для отображения.

Поля класса:
`priceElement: HTMLElement` - поле для хранения элемента разметки, который отображает сумму товаров в корзине.
`orderButton: HTMLButtonElement` - поле для хранения элемента кнопки заказа.
`basketListElement: HTMLElement` - поле для хранения элемента разметки, в который будут размещены сами товары, изначально пустой.

Методы:
`set price(value: string)` - изменяет текстовое содержание элемента суммы стоимости товаров.
`set basketList(items: HTMLElement[])` - принимает объект с элементами, которые содержат товары и добавляет их в корзину.

#### Класс Card
Содержит логику отображения части данных в карточке товара.

Конструктор:  
`constructor(container: HTMLElement)` - В конструктор передается только унаследованный элемент контейнера для отображения.

Поля класса:
`priceElement: HTMLElement` - поле для хранения элемента разметки, который отображает стоимость товара.
`titleElement: HTMLElement` - поле для хранения элемента разметки, который отображает название товара.

Методы:
`set title(value: string)` - изменяет текстовое содержание элемента названия товара.
`set price(value: string)` - изменяет текстовое содержание элемента стоимости товара.

#### Класс CardBasket
Содержит логику отображения данных карточки товара для размещения в корзине.

Конструктор:  
`constructor(protected events: IEvents, container: HTMLElement)` - В конструктор передается обработчик событий и унаследованный элемент контейнера для отображения.

Поля класса:
`indexElement: HTMLElement` - поле для хранения элемента разметки, который отображает порядковый номер товара в корзине.
`cardId: string = ''` - поле для хранения идентификатора товара, которое используется для определения товара, на котором произошло событие.
`deleteButton: HTMLButtonElement` - поле для хранения элемента кнопки удаления товара из корзины.

Методы:
`set index(value: number)` - изменяет текстовое содержание элемента с порядковым номером товара.
`set id(value: string)` - изменяет текстовое содержание элемента с идентификатором товара.

#### Класс CardPreview
Содержит логику отображения наиболее подробных данных карточки товара.

Конструктор:  
`constructor(protected events: IEvents, container: HTMLElement)` - В конструктор передается обработчик событий и унаследованный элемент контейнера для отображения.

Поля класса:
`categoryElement: HTMLElement` - поле для хранения элемента разметки, который отображает категорию товара.
`imageElement: HTMLImageElement` - поле для хранения элемента разметки, который отображает изображение товара.
`textElement: HTMLElement` - поле для хранения элемента разметки, который отображает описание товара.
`addToCartButton` - поле для хранения элемента кнопки добавления или удаления товара из корзины.

Методы:
`set category(value: string)` - изменяет текстовое содержание элемента с категорией товара, также проверяет на соответствие.
`set image(value: string)` - изменяет изображение в соответствующем элементе.
`set text(value: string)` - изменяет текстовое содержание элемента с описанием товара.

#### Класс CardCatalog
Содержит логику отображения данных карточки товара, которая используется для отображения в каталоге главной страницы.

Конструктор:  
`constructor(container: HTMLElement, actions?: ICardActions)` - В конструктор передается обработчик событий и унаследованный элемент контейнера для отображения.

Поля класса:
`categoryElement: HTMLElement` - поле для хранения элемента разметки, который отображает категорию товара.
`imageElement: HTMLImageElement` - поле для хранения элемента разметки, который отображает изображение товара.

Методы:
`set category(value: string)` - изменяет текстовое содержание элемента с категорией товара, также проверяет на соответствие.
`set image(value: string)` - изменяет изображение в соответствующем элементе.

#### Класс Form
Содержит логику отображения данных формы, но в основном тут для отображения ошибок при заполнении формы.

Конструктор:  
`constructor(container: HTMLElement)` - В конструктор передается только унаследованный элемент контейнера для отображения.

Поля класса:
`formElement: HTMLElement` - поле для хранения элемента разметки, который содержит поля формы.
`errorsElement: HTMLElement` - поле для хранения элемента разметки, который содержит ошибки заполнения формы.

Методы:
`set formErrors(error: string)` - изменяет текстовое содержание элемента с ошибками формы, чтобы отобразить их для пользователя.


#### Класс FormContacts
Содержит логику отображения формы с контактными данными.

Конструктор:  
`(protected events: IEvents, container: HTMLElement)` - В конструктор передается только унаследованный элемент контейнера для отображения и обработчик событий, они все почти одинаковые.

Поля класса:
`emailElement: HTMLInputElement` - поле для хранения элемента разметки, который содержит поле формы для ввода почты.
`phoneElement: HTMLInputElement` - поле для хранения элемента разметки, который содержит поле формы для ввода контактного номера телефона.

Методы:
`set email(value: string)` - задает значение почты согласно вводу.
`set phone(value: string)` - тоже, что и предыдущий, но с номером телефона.

#### Класс FormOrder
Содержит логику отображения формы с контактными данными.

Конструктор:  
`(protected events: IEvents, container: HTMLElement)` - В конструктор передается только унаследованный элемент контейнера для отображения и обработчик событий, они все почти одинаковые.

Поля класса:
`addressElement: HTMLInputElement` - поле для хранения элемента разметки, который содержит поле формы для ввода адреса.
`cardButton: HTMLButtonElement` - поле для хранения элемента разметки, который содержит кнопку выбора способы оплаты, эта для онлайн оплаты.
`cashButton: HTMLButtonElement` - следите за руками, две буквы изменились и это поле такое же, как предыдущее, но уже для оплаты при получении.
`nextButton: HTMLButtonElement` - поле для хранения элемента разметки, который содержит кнопку перехода к следующей странице формы.

Методы:
`set adress(value: string)` - задает значение адреса согласно вводу.
`set paymentMethod(value: TPayment)` - тоже, что и предыдущий, но с способом оплаты и эта без текстового ввода, кнопочками.

#### Класс Gallery
С него стоило начать, содержит логику отображения каталога на главной странице.

Конструктор:  
`constructor(container: HTMLElement)` - В конструктор передается только унаследованный элемент контейнера для отображения.

Поля класса:
`catalogElement: HTMLElement` - поле для хранения элемента разметки, в который будут добавляться карточки товара.

Методы:
`set catalog(items: HTMLElement[])` - собственно метод, который добавляет в наш контейнер переданные карточки товаров.

#### Класс Header
Содержит логику отображения кнопки корзинки в шапке сайта.

Конструктор:  
`constructor(protected events: IEvents, container: HTMLElement)` - В конструктор передается только унаследованный элемент контейнера для отображения и обработчик событий.

Поля класса:
`counterElement: HTMLElement` - поле для хранения элемента разметки, который отображает количество товаров в корзине.
`basketButton: HTMLButtonElementt` - поле для хранения элемента самой кнопки корзины.

Методы:
`set counter(value: number)` - изменяет текстовое содержание элемента, который отображает количество товаров в корзине.

#### Класс Modal
Содержит логику отображения всплывающих модальных окон и их зыкрытия.

Конструктор:  
`(protected events: IEvents, container: HTMLElement)` - В конструктор передается только унаследованный элемент контейнера для отображения и обработчик событий.

Поля класса:
`modalElement: HTMLElement` - поле для хранения элемента разметки модального окна.
`contentElement: HTMLElement` - поле для хранения элемента разметки содержимого, которое будет в модальном окне находиться.
`closeButton: HTMLButtonElement` - поле для хранения элемента кнопки закрытия окна.

Методы:
`set content(element: HTMLElement)` - задает наполнение модального окна полученным элементом.

#### Класс Success
Содержит логику отображения подтверждения заказа.

Конструктор:  
`(protected events: IEvents, container: HTMLElement)` - В конструктор передается только унаследованный элемент контейнера для отображения и обработчик событий.

Поля класса:
`priceElement: HTMLElement` - поле для хранения элемента разметки, который отображает стоимость заказа.
`successButton: HTMLButtonElement` - поле для хранения элемента кнопки закрытия окна.

Методы:
`set price(value: string)` - задает значение стоимости заказа.

### События

`items:saved` - событие сохранения товаров и их отображения в каталоге.

`card:active` - событие клика на карточку товара в каталоге, открывает модальное окно с подробным описанием товара.

`card:add` - событие добавления или удаления товара из корзины при клике на кнопку в карточке самого товара.

`basket:open` - событие открытия корзины товаров.

`basket:change` - событие изменения данных в корзине.

`basket:item:removed` - событие удаления товара из корзины при нажатии на кнопку удаления внутри самой корзины.

`order:start` - событие начала оформления заказа по нажатию на кнопку оформления в корзине, открывает первую форму.

`address:change` - событие изменения адреса при вводе в форме.

`order:change` - событие изменения данных заказа.

`payment:card` - событие выбора способа оплаты картой.
`payment:cash` - событие выбора способа оплаты наличными.

`contacts:open` - событие подтверждения данных первой формы и перехода к форме контактной информации.

`email:change` - событие изменения электронной почты при вводе в форме.

`phone:change` - событие изменения номера телефона при вводе в форме.

`contacts:change` - событие изменения данных формы контактной информации.

`success:open` - событие подтверждения заказа и открытия окна об успешном заказе.

`success:close` - событие закрытия экрана успешного заказа при нажатии на кнопку о возврате к покупкам.

`buyer:saved` - событие успешного сохранения данных покупателя.

### Презентер
Написан в коде основного приложения, без выноса в отдельный класс.
